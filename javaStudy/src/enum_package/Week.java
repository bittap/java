package enum_package;


// Enum : 열거타입
// 요일, 계절과 같이 한정된 값만 갖는경우 열거타입을 사용
// enum을 사용하는 이유는
// 예를 들어 월~일 까지 있지만 월화이런 값을 넣지 못하게 하는 효과가 있다.
public enum Week {
	MONDAY,
	TUESDAY,
	WEDNESDA,
	THURSDAY,
	FRIDAY,
	SATURDAY,
	SUNDAY
}
